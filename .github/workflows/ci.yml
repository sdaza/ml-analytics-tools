name: CI

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies
        run: |
          if [ -s system_deps.txt ]; then
            sudo apt-get update
            sudo xargs -r -a system_deps.txt apt-get install -y
          fi

      - name: Install dependencies
        # Include the optional [modeling] extra so the ModelManager / model_tools
        # tests (sklearn, mlflow, ...) can run. The [databricks] extra is left out
        # on purpose: databricks-connect is only for local Spark dev.
        run: uv sync --all-groups --extra modeling --locked

      - name: Lint
        run: uv run ruff check

      - name: Test
        run: uv run pytest
